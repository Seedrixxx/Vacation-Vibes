[build]
  command = "npx prisma generate && npm run build"

# Netlify's Next.js plugin handles SSR, API routes, middleware, and image optimization
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Optional: use a specific Node version (Netlify defaults to 18)
# [build.environment]
#   NODE_VERSION = "18"
